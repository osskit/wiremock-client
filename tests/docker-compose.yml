version: '3.9'

services:
    mocks:
        image: rodolpheche/wiremock
        ports:
            - 8080:8080
        command: '--verbose'
    mocks2:
        image: rodolpheche/wiremock
        ports:
            - 9090:8080
        command: '--verbose'

