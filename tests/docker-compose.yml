version: '3.9'

services:
  mocks:
    image: wiremock/wiremock
    ports:
      - 8080:8080
    command: '--verbose --local-response-templating'
  mocks2:
    image: wiremock/wiremock
    ports:
      - 9090:8080
    command: '--verbose --local-response-templating'
